# Chrome Web Store listing — Tiếng Việt

Translated from `listing.en.md`, keeping its structure. Register: neutral and
friendly, "bạn"; established loanwords kept in English where that is what people
actually say (bookmark, import, export), per `docs/07 §4`.

**Name:** `BookmarkMagic` (brand name — never translated)

**Summary** (125 / 132 characters — only 7 to spare, check after any edit):

```
Nhập, xuất & chỉnh sửa bookmark giữa các trình duyệt — offline, không tài khoản, không cloud. Dữ liệu không rời khỏi máy bạn.
```

**Description:**

```
Chuyển bookmark giữa các trình duyệt theo cách đơn giản nhất: bằng một tệp.
Không tài khoản. Không đồng bộ đám mây. Hoàn toàn offline.

TIỆN ÍCH NÀY LÀM GÌ
• Nhập tệp bookmark (HTML, JSON, CSV) và xem trước đầy đủ trước khi ghi bất
  cứ thứ gì — số lượng, thư mục và các mục trùng lặp đều hiện ra trước.
• Chọn cách nhập: vào một thư mục mới theo ngày (mặc định an toàn), gộp vào
  các thư mục sẵn có, hoặc thay thế toàn bộ (tệp sao lưu an toàn sẽ được tải
  về trước).
• Tự động bỏ qua các mục trùng lặp.
• Xuất toàn bộ — hoặc chỉ những thư mục bạn chọn — ra HTML (dùng được với
  Chrome, Edge, Brave, Firefox, Safari, Vivaldi, Opera), JSON, CSV hoặc
  Markdown.
• Chỉnh sửa cây bookmark: tìm kiếm, đổi tên, kéo thả, tạo thư mục, xoá và tìm
  các liên kết trùng lặp.

RIÊNG TƯ NGAY TỪ THIẾT KẾ
• Chạy hoàn toàn trên máy bạn. Không máy chủ, không phân tích, không theo dõi.
• Chỉ hai quyền: "bookmarks" (chính là mục đích của tiện ích) và "storage"
  (thiết lập của bạn). Không gì khác.
• Miễn phí & mã nguồn mở (GPL-3.0) — bạn có thể đọc mã trên GitHub.

NGÔN NGỮ
English · Tiếng Việt · 日本語

GIỚI HẠN (NÓI THẲNG)
• Không thể nhập favicon — API bookmark của trình duyệt không có trường
  favicon.
• Bookmark được nhập vào sẽ mang ngày hôm nay — Chrome không cho phép tiện ích
  đặt lại ngày tạo gốc của một bookmark. Các tệp bạn xuất ra vẫn giữ ngày thật.
• CSV là dạng phẳng: thư mục rỗng và thông tin "thư mục nào là thanh bookmark"
  sẽ không được giữ lại. Hãy dùng HTML hoặc JSON nếu muốn sao lưu trung thực.

Mã nguồn, nơi báo lỗi & chính sách riêng tư:
https://github.com/poli0981/bookmarkmagic
```

Lưu ý: các tài liệu pháp lý chỉ có bản tiếng Anh (`docs/14`), nên URL chính sách
riêng tư giống hệt bản EN.
